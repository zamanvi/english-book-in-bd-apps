<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_deep"
    tools:context=".Activity.SplashActivity">

    <!-- Decorative top glow -->
    <View
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:background="@drawable/splash_glow_top"
        android:alpha="0.35"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="-80dp"/>

    <!-- Center content block -->
    <LinearLayout
        android:id="@+id/centerBlock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:alpha="0"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- App icon with glow card -->
        <androidx.cardview.widget.CardView
            android:layout_width="130dp"
            android:layout_height="130dp"
            app:cardCornerRadius="32dp"
            app:cardElevation="24dp"
            app:cardBackgroundColor="#0F172A">
            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@mipmap/ic_launcher"
                android:contentDescription="@string/app_name" />
        </androidx.cardview.widget.CardView>

        <!-- App name Bengali -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="বাংলাদেশের সেরা"
            android:textColor="#2DD4BF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:letterSpacing="0.02"
            tools:ignore="HardcodedText" />

        <!-- Grammar Book big title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="English Grammar বই"
            android:textColor="@color/text_primary"
            android:textSize="26sp"
            android:textStyle="bold"
            tools:ignore="HardcodedText" />

        <!-- Tagline -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="ভর্তি ও চাকরির পরীক্ষায় সেরা প্রস্তুতি"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            tools:ignore="HardcodedText" />

        <!-- Divider line -->
        <View
            android:layout_width="40dp"
            android:layout_height="2dp"
            android:layout_marginTop="20dp"
            android:background="#2DD4BF"
            android:alpha="0.5"/>

    </LinearLayout>

    <!-- Bottom section: progress + version -->
    <LinearLayout
        android:id="@+id/bottomBlock"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:alpha="0"
        android:layout_marginBottom="48dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Thin animated progress bar -->
        <ProgressBar
            android:id="@+id/setProgressBarId"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="120dp"
            android:layout_height="2dp"
            android:indeterminate="true"
            android:progressTint="#2DD4BF"
            android:indeterminateTint="#2DD4BF"
            android:progressBackgroundTint="@color/bg_elevated" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="অনুগ্রহ করে অপেক্ষা করুন..."
            android:textColor="@color/text_muted"
            android:textSize="11sp"
            tools:ignore="HardcodedText" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="v1.4.0"
            android:textColor="@color/text_inactive"
            android:textSize="10sp"
            android:letterSpacing="0.05"
            tools:ignore="HardcodedText" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
